<?xml version="1.0" encoding="utf-8"?>

<odoo noupdate="1">
    <record id="access_holder_user_rule" model="ir.rule">
        <field name="name">access holder user rule"</field>
        <field name="model_id" ref="model_accesses_storage"/>
        <field name="domain_force">['|',('user_ids','in',[user.id]),('create_uid','=',user.id)]</field>
    </record>
</odoo>

