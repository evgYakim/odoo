<?xml version='1.0'?>
<odoo>
    <act_window id="action_accesses_holder"
                name="access"
                res_model="accesses.storage"
                view_mode="kanban,form"
    />

    <menuitem id="accesses_holder_menu"
              name="accesses"
              action="action_accesses_holder"
    />

    <record id="view_accesses_holder" model="ir.ui.view">
        <field name="name">Accesses holder form</field>
        <field name="model">accesses.storage</field>
        <field name="arch" type="xml">
            <form string="access holder">
                <sheet>
                    <group>
                        <group name="group_left">
                            <field name="name"/>
                            <field name="project_id" mode="tree"/>
                        </group>
                        <field name="data" widget="html"/>
                        <group name="group_right">
                            <field name="user_ids">
                                <form string="user">
                                    <field name="name"/>
                                </form>
                                <tree string="user" editable="bottom">
                                    <field name="name"/>
                                </tree>
                            </field>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_accesses_holder_kanban" model="ir.ui.view">
        <field name="name">Accesses holder kanban</field>
        <field name="model">accesses.storage</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column">
                <field name="color"/>
                <field name="kanban_state"/>
                <field name="project_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="#{kanban_color(record.color.raw_value)} oe_kanban_global_click">
                            <div class="o_dropdown_kanban dropdown">
                                <a class="dropdown-toggle btn" data-toggle="dropdown" href="#" >
                                    <span class="fa fa-bars fa-lg"/>
                                </a>
                                <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                                    <t t-if="widget.editable"><li><a type="edit">Edit</a></li></t>
                                    <t t-if="widget.deletable"><li><a type="delete">Delete</a></li></t>
                                    <li><ul class="oe_kanban_colorpicker" data-field="color"/></li>
                                </ul>
                            </div>
                            <div class="oe_kanban_content">
                                <div>
                                    <strong>accesses for project:</strong><br/>
                                    <field name="project_id"/>
                                </div>
                            </div>
                            <div class="oe_clear"></div>
                        </div>
                    </t>
                </templates>

            </kanban>
        </field>

    </record>


</odoo>